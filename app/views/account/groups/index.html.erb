<div class="col-md-12">
  
  <h2 class="text-center"> 課程 </h2>
  
  <table class="table">
    <thead>
      <tr>
        <th> # </th>
        <th> Title </th>
        <th> Description</th>
       
       
      </tr>
    </thead>
    
    <tbody>
      <% @groups.each do |group| %>
        <tr>
          <td> # </td>
          <td> <%= link_to(group.title, group_path(group)) %> </td> 
          <td> <%= group.description %> </td> 
         
        </tr>
      <% end %>
    </tbody>
  </table>
</div>